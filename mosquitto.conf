# Listener para WebSockets (porta 8080 — Render usa essa para HTTP)
listener 8080
protocol websockets
allow_anonymous true

# (Opcional) Listener interno MQTT normal para outros serviços no Render
listener 1883
allow_anonymous true

# Logs para STDOUT (Render mostra nos logs)
log_type all
